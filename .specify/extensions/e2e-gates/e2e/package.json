{
  "name": "e2e-gates",
  "private": true,
  "type": "module",
  "scripts": {
    "gate": "./run-gates.sh",
    "gate:headed": "./run-gates.sh --headed",
    "gate:ci": "./run-gates.sh --ci",
    "install:browsers": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "imapflow": "^1.0.171"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "playwright-ctrf-json-reporter": "^0.0.9",
    "@axe-core/playwright": "^4.10.0"
  }
}
