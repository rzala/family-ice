/** Demo destination + start presets (Constitution Principle V — canonical Kiskunlácháza demo). */
export const DESTINATION = { name: 'kert-utca', lat: 47.1754743, lng: 18.9970884 };

export const START_PRESETS: Record<string, { lat: number; lng: number }> = {
  // ~2.5 km north of Kert utca 14, on the approach into Pereg/Kiskunlácháza.
  'north-approach': { lat: 47.198, lng: 18.9971 },
};
